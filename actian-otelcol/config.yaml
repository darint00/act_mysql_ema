receivers:
  otlp:
    protocols:
      grpc:
  ema:
    interval: 5s
    number_of_traces: 1

processors:

exporters:
  logging:
    loglevel: debug

service:
  pipelines:
    metrics:
      receivers: [otlp,ema]
      processors: []
      exporters: [logging]  
